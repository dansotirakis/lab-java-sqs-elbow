server:
  port: 8082
spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: none
  datasource:
    url: ${DS_URL}
    username: ${DS_USR}
    password: ${DS_PASS}
aws:
  url:
    queue: ${QUEUE_URL}